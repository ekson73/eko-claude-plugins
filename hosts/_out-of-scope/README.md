# Out of scope for eko catalog “ready/docs install”

| Target | status | Why |
|---|---|---|
| **Grok / xAI** | `n/a` | **Model provider**, not a pack host. Use via Pi/Claude/etc. model settings. |
| **Meta Muse Code** | `n/a` | Agent product; no stable public third-party plugin/package registry eko can index honestly (community awesome/prompt dumps ≠ registry). |
| **ChatGPT store** | `n/a` | Controlled by OpenAI; see `hosts/openai`. |
| **VS Code Marketplace as eko product** | `n/a` | Wrong abstraction; see `hosts/microsoft`. |

These stay in `registry/catalog.json` as explicit `n/a` so amnesic agents do not invent support.
